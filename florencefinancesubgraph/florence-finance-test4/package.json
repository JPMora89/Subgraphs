{
  "name": "florence-finance-test4",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ florence-finance-test4",
    "create-local": "graph create --node http://localhost:8020/ florence-finance-test4",
    "remove-local": "graph remove --node http://localhost:8020/ florence-finance-test4",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 florence-finance-test4",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.61.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
